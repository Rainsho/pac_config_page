{
  "name": "pac",
  "version": "1.1.0",
  "description": "pac_config_tool",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "export NODE_ENV=production && node index.js",
    "dev": "export NODE_ENV=development && node index.js",
    "watch": "export NODE_ENV=development && supervisor index.js",
    "build": "cd views && npm run build",
    "deploy": "scp -r views/build maru@192.168.100.200:App/pac/views"
  },
  "author": "Rainsho",
  "license": "ISC",
  "dependencies": {
    "@koa/cors": "^2.2.2",
    "diskusage": "^0.2.6",
    "fs-extra": "^7.0.1",
    "koa": "^2.6.2",
    "koa-bodyparser": "^4.2.1",
    "koa-router": "^7.4.0",
    "koa-static": "^5.0.0",
    "node-fetch": "^2.3.0",
    "node-schedule": "^1.3.1",
    "ping": "0.2.2"
  },
  "devDependencies": {
    "supervisor": "^0.12.0"
  }
}
